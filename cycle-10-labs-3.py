# Author: SMR (AMDG) 2/1/22
# Defining the function
def reverse(list):
# Using the .reverse command    
    list.reverse()
# Then returning the list given except its reversed now    
    return list
# Testing
print(reverse(["My", "name", "is", "Sean"]))
